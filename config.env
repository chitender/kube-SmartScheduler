# Smart Scheduler Build Configuration
# Source this file: source config.env

# Image Registry and Repository (configured for your GitHub)
export REGISTRY=ghcr.io
export REPOSITORY=chitender/kube-smartscheduler

# Version (override automatic git detection if needed)
# export VERSION=v1.0.2

# GitHub Container Registry Authentication
# Get your personal access token from: https://github.com/settings/tokens
# Enable "write:packages" permission
# export GITHUB_TOKEN=ghp_your_token_here

# Build Configuration
export DOCKER_BUILDKIT=1

echo "✅ Smart Scheduler build configuration loaded"
echo "Registry: $REGISTRY"
echo "Repository: $REPOSITORY"
echo "Current version: $(git describe --tags --always --dirty 2>/dev/null || echo 'v0.1.0')" 